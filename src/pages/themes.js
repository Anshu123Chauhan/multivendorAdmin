import React from "react";
import Layout, { Container } from "../components/layout";
import BackHeader from "../components/backHeader";

const Themes = () => {
  return (
    <Layout>
      <Container><BackHeader title="Inventory" /></Container>
    </Layout>
  );
};

export default Themes;
